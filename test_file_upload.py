#!/usr/bin/env python3
"""
ไฟล์ทดสอบการอัปโหลดไฟล์
สร้างไฟล์ PDF ตัวอย่างสำหรับทดสอบ
"""

import os

def create_test_pdf():
    """สร้างไฟล์ PDF ตัวอย่าง"""
    # สร้างโฟลเดอร์ test
    test_folder = 'test_files'
    os.makedirs(test_folder, exist_ok=True)
    
    # สร้างไฟล์ PDF ตัวอย่าง (ไฟล์ข้อความธรรมดา)
    pdf_content = """%PDF-1.4
1 0 obj
<<
/Type /Catalog
/Pages 2 0 R
>>
endobj

2 0 obj
<<
/Type /Pages
/Kids [3 0 R]
/Count 1
>>
endobj

3 0 obj
<<
/Type /Page
/Parent 2 0 R
/MediaBox [0 0 612 792]
/Contents 4 0 R
>>
endobj

4 0 obj
<<
/Length 44
>>
stream
BT
/F1 12 Tf
72 720 Td
(ไฟล์ทดสอบการอัปโหลด) Tj
ET
endstream
endobj

xref
0 5
0000000000 65535 f 
0000000009 00000 n 
0000000058 00000 n 
0000000115 00000 n 
0000000204 00000 n 
trailer
<<
/Size 5
/Root 1 0 R
>>
startxref
297
%%EOF"""
    
    # บันทึกไฟล์
    test_file_path = os.path.join(test_folder, 'test_upload.pdf')
    with open(test_file_path, 'w', encoding='utf-8') as f:
        f.write(pdf_content)
    
    print(f"✅ สร้างไฟล์ทดสอบสำเร็จ: {test_file_path}")
    print(f"📁 ขนาดไฟล์: {len(pdf_content)} bytes")
    print(f"🔗 ใช้สำหรับทดสอบการอัปโหลดในระบบ")
    
    return test_file_path

def create_test_image():
    """สร้างไฟล์รูปภาพตัวอย่าง"""
    # สร้างไฟล์รูปภาพตัวอย่าง (ไฟล์ข้อความธรรมดา)
    image_content = """P3
# ไฟล์รูปภาพทดสอบ
3 2
255
255 0 0
0 255 0
0 0 255
255 255 0
255 0 255
0 255 255"""
    
    # บันทึกไฟล์
    test_file_path = os.path.join('test_files', 'test_image.ppm')
    with open(test_file_path, 'w', encoding='utf-8') as f:
        f.write(image_content)
    
    print(f"✅ สร้างไฟล์รูปภาพทดสอบสำเร็จ: {test_file_path}")
    return test_file_path

def create_test_document():
    """สร้างไฟล์เอกสารตัวอย่าง"""
    # สร้างไฟล์เอกสารตัวอย่าง
    doc_content = """ไฟล์ทดสอบการอัปโหลด

ข้อมูลผู้ป่วย:
- HN: 123456
- ชื่อ: สมชาย
- นามสกุล: แมนเมือง
- หน่วยงาน: หน่วยเบาหวาน

หมายเหตุ: ไฟล์นี้ใช้สำหรับทดสอบการอัปโหลดในระบบจัดการข้อมูลผู้ป่วย

วันที่สร้าง: 2025-08-28
สร้างโดย: ระบบทดสอบ"""
    
    # บันทึกไฟล์
    test_file_path = os.path.join('test_files', 'test_document.txt')
    with open(test_file_path, 'w', encoding='utf-8') as f:
        f.write(doc_content)
    
    print(f"✅ สร้างไฟล์เอกสารทดสอบสำเร็จ: {test_file_path}")
    return test_file_path

if __name__ == "__main__":
    print("🧪 สร้างไฟล์ทดสอบสำหรับระบบจัดการข้อมูลผู้ป่วย")
    print("=" * 60)
    
    try:
        # สร้างไฟล์ทดสอบต่างๆ
        create_test_pdf()
        create_test_image()
        create_test_document()
        
        print("\n" + "=" * 60)
        print("🎉 สร้างไฟล์ทดสอบเสร็จสิ้น!")
        print("\n📋 ไฟล์ที่สร้างขึ้น:")
        print("   📄 test_upload.pdf - ไฟล์ PDF ทดสอบ")
        print("   🖼️ test_image.ppm - ไฟล์รูปภาพทดสอบ")
        print("   📝 test_document.txt - ไฟล์เอกสารทดสอบ")
        
        print("\n🔗 วิธีใช้งาน:")
        print("   1. ไปที่หน้าแก้ไขข้อมูลผู้ป่วย")
        print("   2. เลือก 'แก้ไขไฟล์'")
        print("   3. เลือกไฟล์ทดสอบจากโฟลเดอร์ test_files")
        print("   4. กดบันทึก")
        
        print("\n⚠️ หมายเหตุ:")
        print("   - ไฟล์เหล่านี้เป็นไฟล์ทดสอบเท่านั้น")
        print("   - ไม่มีเนื้อหาจริง")
        print("   - ใช้สำหรับทดสอบระบบเท่านั้น")
        
    except Exception as e:
        print(f"❌ เกิดข้อผิดพลาด: {e}")
