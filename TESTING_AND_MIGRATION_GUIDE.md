# 🧪 คู่มือการทดสอบและการ Migration ระบบจัดการข้อมูลผู้ป่วย

## 📋 สารบัญ
1. [ภาพรวมไฟล์ Python](#ภาพรวมไฟล์-python)
2. [การจัดระเบียบโฟลเดอร์ Test](#การจัดระเบียบโฟลเดอร์-test)
3. [คู่มือการใช้งานไฟล์ต่างๆ](#คู่มือการใช้งานไฟล์ต่างๆ)
4. [ขั้นตอนการทดสอบระบบ](#ขั้นตอนการทดสอบระบบ)
5. [การแก้ไขปัญหา](#การแก้ไขปัญหา)

---

## 🎯 ภาพรวมไฟล์ Python

ระบบจัดการข้อมูลผู้ป่วยประกอบด้วยไฟล์ Python หลายไฟล์ที่ใช้สำหรับการทดสอบ การ Migration และการบำรุงรักษา:

| ไฟล์ | วัตถุประสงค์ | การใช้งาน |
|------|-------------|-----------|
| `migrate_cases.py` | สร้างตารางใหม่สำหรับระบบจัดการข้อมูลผู้ป่วย | ใช้ครั้งแรกเพื่อสร้างโครงสร้างฐานข้อมูล |
| `migrate_cases_documents.py` | เพิ่มฟิลด์เอกสารในตาราง patient_case | ใช้เมื่อต้องการเพิ่มฟีเจอร์เอกสาร |
| `test_cases.py` | ทดสอบระบบจัดการข้อมูลผู้ป่วย | ใช้เพื่อตรวจสอบการทำงานของระบบ |
| `optimize_db.py` | เพิ่มประสิทธิภาพฐานข้อมูล | ใช้เพื่อปรับปรุงประสิทธิภาพ |
| `read_db.py` | อ่านและวิเคราะห์ฐานข้อมูล | ใช้เพื่อตรวจสอบข้อมูลในฐานข้อมูล |

---

## 📁 การจัดระเบียบโฟลเดอร์ Test

แนะนำให้จัดระเบียบโฟลเดอร์ดังนี้:

```
HA_file_final/
├── tests/                          # โฟลเดอร์หลักสำหรับการทดสอบ
│   ├── __init__.py                # ทำให้เป็น Python package
│   ├── unit/                      # การทดสอบหน่วยย่อย
│   │   ├── __init__.py
│   │   ├── test_models.py         # ทดสอบ Models
│   │   ├── test_routes.py         # ทดสอบ Routes
│   │   └── test_utils.py          # ทดสอบ Utility functions
│   ├── integration/               # การทดสอบการทำงานร่วมกัน
│   │   ├── __init__.py
│   │   ├── test_api.py            # ทดสอบ API endpoints
│   │   └── test_database.py       # ทดสอบการทำงานกับฐานข้อมูล
│   ├── fixtures/                  # ข้อมูลทดสอบ
│   │   ├── test_data.sql          # SQL สำหรับสร้างข้อมูลทดสอบ
│   │   └── sample_files/          # ไฟล์ตัวอย่างสำหรับอัปโหลด
│   ├── migrations/                # Scripts สำหรับ Migration
│   │   ├── __init__.py
│   │   ├── migrate_cases.py       # Migration หลัก
│   │   └── migrate_cases_documents.py
│   └── utils/                     # เครื่องมือช่วยการทดสอบ
│       ├── __init__.py
│       ├── test_helpers.py        # ฟังก์ชันช่วยการทดสอบ
│       └── db_setup.py            # การตั้งค่าฐานข้อมูลทดสอบ
├── scripts/                       # Scripts สำหรับการบำรุงรักษา
│   ├── optimize_db.py            # ปรับปรุงประสิทธิภาพฐานข้อมูล
│   ├── read_db.py                # อ่านและวิเคราะห์ฐานข้อมูล
│   └── backup_db.py              # สำรองฐานข้อมูล
└── requirements-test.txt          # Dependencies สำหรับการทดสอบ
```

---

## 📚 คู่มือการใช้งานไฟล์ต่างๆ

### 1. 🚀 `migrate_cases.py` - สร้างตารางใหม่

**วัตถุประสงค์**: สร้างตารางใหม่สำหรับระบบจัดการข้อมูลผู้ป่วย

**การใช้งาน**:
```bash
# รันจากโฟลเดอร์หลัก
python migrate_cases.py
```

**ฟีเจอร์**:
- สร้างตาราง `patient_case` และ `case_audit`
- สร้าง indexes สำหรับประสิทธิภาพ
- สร้าง unique constraint เพื่อป้องกัน HN ซ้ำ
- ตรวจสอบข้อมูลที่มีอยู่

**ผลลัพธ์ที่คาดหวัง**:
```
🏥 ระบบจัดการข้อมูลผู้ป่วย - Database Migration
==================================================
🔧 เริ่มต้นการสร้างตารางใหม่...
✅ สร้างตาราง patient_case สำเร็จ
✅ สร้างตาราง case_audit สำเร็จ
✅ สร้าง index สำหรับ department และ date
✅ สร้าง index สำหรับ date
✅ สร้าง index สำหรับ HN และ date
✅ สร้าง index สำหรับ audit case_id
✅ สร้าง unique constraint สำหรับ HN และ date
✅ บันทึกการเปลี่ยนแปลงลงฐานข้อมูลสำเร็จ
```

### 2. 📄 `migrate_cases_documents.py` - เพิ่มฟิลด์เอกสาร

**วัตถุประสงค์**: เพิ่มฟิลด์สำหรับเก็บข้อมูลเอกสารในตาราง `patient_case`

**การใช้งาน**:
```bash
# รันจากโฟลเดอร์หลัก
python migrate_cases_documents.py
```

**ฟีเจอร์**:
- เพิ่มฟิลด์ `file_path`, `file_size`, `external_link`, `link_type`
- ตรวจสอบว่าฟิลด์มีอยู่แล้วหรือไม่
- แสดงโครงสร้างตารางปัจจุบัน

**ผลลัพธ์ที่คาดหวัง**:
```
🚀 เริ่มต้น Migration สำหรับฟิลด์เอกสาร...
✅ เพิ่มฟิลด์ file_path สำเร็จ
✅ เพิ่มฟิลด์ file_size สำเร็จ
✅ เพิ่มฟิลด์ external_link สำเร็จ
✅ เพิ่มฟิลด์ link_type สำเร็จ
🎉 Migration เสร็จสิ้น!
```

### 3. 🧪 `test_cases.py` - ทดสอบระบบ

**วัตถุประสงค์**: ทดสอบการทำงานของระบบจัดการข้อมูลผู้ป่วย

**การใช้งาน**:
```bash
# รันจากโฟลเดอร์หลัก
python test_cases.py
```

**ฟีเจอร์**:
- ทดสอบ Public Statistics API
- ทดสอบ Public Dashboard
- ทดสอบ Admin Login
- ทดสอบหน้าแรก
- ทดสอบการเชื่อมต่อฐานข้อมูล
- สร้างข้อมูลทดสอบ

**ข้อกำหนด**:
- ต้องรัน Flask app ก่อน (`python app.py`)
- ต้องมีฐานข้อมูลที่สร้างแล้ว
- ต้องมี admin user (username: admin, password: admin123)

**ผลลัพธ์ที่คาดหวัง**:
```
🧪 ระบบทดสอบ - ระบบจัดการข้อมูลผู้ป่วย
==================================================
🔍 ทดสอบหน้าแรก...
✅ หน้าแรกเข้าถึงได้
✅ ลิงก์สถิติแสดงผลถูกต้อง
🔍 ทดสอบ Public Dashboard...
✅ Public Dashboard เข้าถึงได้
✅ เนื้อหาถูกต้อง
🔍 ทดสอบ Public Statistics API...
✅ Stats API สำเร็จ: 10 cases
✅ Trends API สำเร็จ: 6 เดือน
🔍 ทดสอบ Admin Login...
✅ หน้า Login เข้าถึงได้
🔍 ทดสอบการเชื่อมต่อฐานข้อมูล...
✅ ตาราง cases พบ: 2 ตาราง
✅ จำนวนผู้ป่วย: 10 ราย
🔍 สร้างข้อมูลทดสอบ...
✅ มีข้อมูลอยู่แล้ว: 10 ราย
```

### 4. ⚡ `optimize_db.py` - ปรับปรุงประสิทธิภาพฐานข้อมูล

**วัตถุประสงค์**: เพิ่มประสิทธิภาพฐานข้อมูลและวิเคราะห์ประสิทธิภาพ

**การใช้งาน**:
```bash
# รันจากโฟลเดอร์หลัก
python optimize_db.py
```

**ฟีเจอร์**:
- สร้าง indexes สำหรับตารางต่างๆ
- วิเคราะห์ขนาดฐานข้อมูล
- ทดสอบประสิทธิภาพการค้นหา
- ให้คำแนะนำการบำรุงรักษา

**ผลลัพธ์ที่คาดหวัง**:
```
🏥 Database Optimization Script for Hospital Management System
======================================================================
🔧 กำลังสร้างดัชนีเพื่อเพิ่มประสิทธิภาพ...
  ✅ สร้างดัชนี: department_code
  ✅ สร้างดัชนี: department_name
  ✅ สร้างดัชนี: guideline_dept
  ✅ สร้างดัชนี: knowledge_dept
  ✅ สร้างดัชนี: activity_dept
  ✅ สร้างดัชนี: contact_dept
  ✅ สร้างดัชนี: admin_username
✅ สร้างดัชนีเสร็จสิ้น

📊 การวิเคราะห์ฐานข้อมูล
==================================================
📁 ขนาดไฟล์ฐานข้อมูล: 0.25 MB
📋 Department: 5 รายการ
📋 Admin_user: 1 รายการ
📋 Guideline: 3 รายการ
📋 Knowledge: 5 รายการ
📋 Activity: 5 รายการ
📋 Contact: 5 รายการ

📈 จำนวนข้อมูลทั้งหมด: 24 รายการ
📏 ขนาดเฉลี่ยต่อรายการ: 10,922.67 bytes
```

### 5. 📖 `read_db.py` - อ่านและวิเคราะห์ฐานข้อมูล

**วัตถุประสงค์**: อ่านและแสดงข้อมูลในฐานข้อมูล

**การใช้งาน**:
```bash
# รันจากโฟลเดอร์หลัก
python read_db.py
```

**ฟีเจอร์**:
- แสดงโครงสร้างตาราง
- แสดงจำนวนข้อมูลในแต่ละตาราง
- แสดงข้อมูลทั้งหมดในแต่ละตาราง
- แสดงสรุปข้อมูลหน่วยงาน

**ผลลัพธ์ที่คาดหวัง**:
```
🏥 SQLite Database Reader for Hospital Management System
============================================================
📋 โครงสร้างตาราง: department
--------------------------------------------------
  id (INTEGER) - NOT NULL
  code (VARCHAR) - NOT NULL
  name (VARCHAR) - NOT NULL
  description (TEXT) - NULL
  created_at (DATETIME) - NOT NULL
  updated_at (DATETIME) - NOT NULL

📊 จำนวนข้อมูล: 5 รายการ

📝 ข้อมูลในตาราง department:
--------------------------------------------------

  รายการที่ 1:
    id: 1
    code: DM
    name: หน่วยเบาหวาน
    description: หน่วยงานดูแลผู้ป่วยเบาหวาน
    created_at: 2024-01-15 10:00:00
    updated_at: 2024-01-15 10:00:00
```

---

## 🔄 ขั้นตอนการทดสอบระบบ

### ขั้นตอนที่ 1: เตรียมระบบ
```bash
# 1. สร้างฐานข้อมูล
python migrate_cases.py

# 2. เพิ่มฟิลด์เอกสาร (ถ้าจำเป็น)
python migrate_cases_documents.py

# 3. รัน Flask app
python app.py
```

### ขั้นตอนที่ 2: ทดสอบระบบ
```bash
# 4. ทดสอบการทำงานของระบบ
python test_cases.py

# 5. ตรวจสอบฐานข้อมูล
python read_db.py

# 6. ปรับปรุงประสิทธิภาพ (ถ้าจำเป็น)
python optimize_db.py
```

### ขั้นตอนที่ 3: ตรวจสอบผลลัพธ์
- ตรวจสอบผลลัพธ์จากแต่ละ script
- ตรวจสอบการทำงานของเว็บแอป
- ตรวจสอบฐานข้อมูล

---

## 🛠️ การแก้ไขปัญหา

### ปัญหาที่พบบ่อย

#### 1. ไม่พบฐานข้อมูล
**อาการ**: `❌ ไม่พบฐานข้อมูล`
**วิธีแก้**: รัน `python migrate_cases.py` ก่อน

#### 2. Flask app ไม่รัน
**อาการ**: `Connection refused` เมื่อรัน test
**วิธีแก้**: ตรวจสอบว่า Flask app รันอยู่ที่ port 5001

#### 3. ตารางไม่ถูกสร้าง
**อาการ**: `❌ ไม่พบตาราง cases`
**วิธีแก้**: ตรวจสอบ error ใน `migrate_cases.py`

#### 4. Permission denied
**อาการ**: ไม่สามารถเขียนไฟล์ได้
**วิธีแก้**: ตรวจสอบสิทธิ์การเขียนในโฟลเดอร์

### การ Debug

#### 1. ตรวจสอบ Logs
```bash
# ดู error ใน Flask app
python app.py

# ดู error ใน migration scripts
python migrate_cases.py 2>&1 | tee migration.log
```

#### 2. ตรวจสอบฐานข้อมูล
```bash
# ดูโครงสร้างตาราง
python read_db.py

# ใช้ SQLite command line
sqlite3 instance/hospital.db
.tables
.schema patient_case
```

#### 3. ตรวจสอบการเชื่อมต่อ
```bash
# ทดสอบการเชื่อมต่อ
python -c "
import sqlite3
conn = sqlite3.connect('instance/hospital.db')
print('เชื่อมต่อสำเร็จ')
conn.close()
"
```

---

## 📝 หมายเหตุสำคัญ

### 1. การ Backup
- สำรองฐานข้อมูลก่อนรัน migration scripts
- ใช้ `sqlite3 instance/hospital.db .backup backup.db`

### 2. การ Rollback
- Migration scripts ไม่มี rollback
- สำรองข้อมูลก่อนรันเสมอ

### 3. การทดสอบใน Production
- ไม่ควรรัน test scripts ใน production
- ใช้ staging environment สำหรับการทดสอบ

### 4. การอัปเดต Dependencies
- อัปเดต `requirements.txt` เมื่อเพิ่ม dependencies ใหม่
- ใช้ virtual environment เสมอ

---

## 🎯 สรุป

ไฟล์ Python ทั้งหมดนี้ช่วยในการ:
1. **สร้างและอัปเดตฐานข้อมูล** (migrate scripts)
2. **ทดสอบการทำงานของระบบ** (test scripts)
3. **ปรับปรุงประสิทธิภาพ** (optimize scripts)
4. **ตรวจสอบและวิเคราะห์ข้อมูล** (read scripts)

การจัดระเบียบโฟลเดอร์ test จะช่วยให้:
- การทดสอบเป็นระบบมากขึ้น
- ง่ายต่อการบำรุงรักษา
- แยกการทดสอบตามประเภท
- มีโครงสร้างที่ชัดเจน

---

## 📞 การติดต่อ

หากมีปัญหาหรือคำถามเพิ่มเติม กรุณาติดต่อทีมพัฒนา หรือสร้าง issue ในระบบ version control
