# Environment Variables สำหรับ Vercel
# คัดลอกค่าเหล่านี้ไปใส่ใน Vercel Dashboard → Settings → Environment Variables

# Flask Configuration
FLASK_ENV=production
FLASK_DEBUG=False

# Security (สำคัญมาก! เปลี่ยนเป็นค่าที่ปลอดภัย)
# สร้างด้วย: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=<สร้างใหม่ด้วยคำสั่งด้านบน>

# Database - Supabase จาก Coolify
# แทนที่ [PASSWORD] และ [HOST] ด้วยค่าจริงจาก Supabase
DATABASE_URL=postgresql://postgres:[PASSWORD]@[SUPABASE-HOST]:5432/postgres

# CORS (ใช้ URL ที่ Vercel ให้มา หลังจาก deploy แล้ว)
CORS_ORIGINS=https://your-app.vercel.app

# Vercel
VERCEL=1

# Upload Settings (ปรับสำหรับ serverless)
UPLOAD_FOLDER=supabase_storage
MAX_CONTENT_LENGTH=10485760

# Backup Settings (ไม่ใช้ใน serverless)
# BACKUP_DIR=storage/backups
# BACKUP_INTERVAL_HOURS=24
# BACKUP_KEEP_DAYS=30


