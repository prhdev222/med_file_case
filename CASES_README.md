# ระบบจัดการข้อมูลผู้ป่วยใหม่ - แผนกอายุรกรรม

## 📋 ภาพรวม

ระบบจัดการข้อมูลผู้ป่วยใหม่เป็นส่วนขยายของระบบจัดการไฟล์แผนกอายุรกรรมเดิม โดยเพิ่มความสามารถในการ:

- บันทึกข้อมูลผู้ป่วยใหม่ (HN, ชื่อ, นามสกุล, หน่วยงาน, วันที่)
- ดูสถิติผู้ป่วยใหม่แบบ real-time
- จัดการข้อมูลผู้ป่วยสำหรับ admin
- Export ข้อมูลเป็น CSV
- Audit trail สำหรับการเปลี่ยนแปลงข้อมูล

## 🚀 การติดตั้งและใช้งาน

### 1. รัน Database Migration

```bash
python migrate_cases.py
```

### 2. รันระบบ

```bash
python app.py
```

### 3. เข้าสู่ระบบ

- **Public Dashboard**: `/stats` - ดูสถิติผู้ป่วยใหม่ (ไม่ต้อง login)
- **Admin Panel**: `/admin/login` - เข้าสู่ระบบเพื่อจัดการข้อมูล

## 🏗️ โครงสร้างระบบ

### Models

#### PatientCase
```python
class PatientCase(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    hn = db.Column(db.String(20), nullable=False)  # Hospital Number
    first_name = db.Column(db.String(100), nullable=False)
    last_name = db.Column(db.String(100), nullable=False)
    department_id = db.Column(db.Integer, db.ForeignKey('department.id'))
    case_date = db.Column(db.Date, nullable=False)
    notes = db.Column(db.Text)
    created_at = db.Column(db.DateTime, default=datetime.now)
    created_by = db.Column(db.Integer, db.ForeignKey('admin_user.id'))
    updated_at = db.Column(db.DateTime, default=datetime.now)
    is_deleted = db.Column(db.Boolean, default=False)
```

#### CaseAudit
```python
class CaseAudit(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    case_id = db.Column(db.Integer, db.ForeignKey('patient_case.id'))
    action = db.Column(db.String(20), nullable=False)  # CREATE, UPDATE, DELETE
    user_id = db.Column(db.Integer, db.ForeignKey('admin_user.id'))
    ip_address = db.Column(db.String(45))
    created_at = db.Column(db.DateTime, default=datetime.now)
```

### Routes

#### Public Routes
- `GET /stats` - หน้าแสดงสถิติสาธารณะ
- `GET /api/public/stats` - API สำหรับข้อมูลสถิติ
- `GET /api/public/trends` - API สำหรับข้อมูลแนวโน้ม

#### Admin Routes
- `GET /admin/cases` - จัดการข้อมูลผู้ป่วย
- `GET /admin/cases/add` - เพิ่มผู้ป่วยใหม่
- `GET /admin/cases/edit/<id>` - แก้ไขข้อมูลผู้ป่วย
- `POST /admin/cases/delete/<id>` - ลบข้อมูลผู้ป่วย
- `GET /admin/cases/export` - Export ข้อมูลเป็น CSV

## 📊 ฟีเจอร์หลัก

### 1. Public Dashboard (`/stats`)

#### สถิติรวม
- จำนวนผู้ป่วยใหม่ตามช่วงเวลา
- การเปลี่ยนแปลงเปรียบเทียบกับช่วงก่อนหน้า
- การกระจายตามหน่วยงาน

#### Charts
- **Pie Chart**: การกระจายตามหน่วยงาน
- **Bar Chart**: เปรียบเทียบระหว่างหน่วยงาน
- **Line Chart**: แนวโน้ม 6 เดือนย้อนหลัง

#### Filters
- ช่วงเวลา: สัปดาห์, เดือน, 3 เดือน, 6 เดือน, ปี
- หน่วยงาน: เลือกเฉพาะหน่วยงานหรือดูทั้งหมด

### 2. Admin Management

#### จัดการข้อมูลผู้ป่วย
- เพิ่มผู้ป่วยใหม่
- แก้ไขข้อมูลผู้ป่วย
- ลบข้อมูลผู้ป่วย (soft delete)
- ค้นหาและกรองข้อมูล

#### Export ข้อมูล
- Export เป็น CSV
- เลือกช่วงวันที่
- เลือกหน่วยงานเฉพาะ

#### Audit Trail
- บันทึกการสร้าง/แก้ไข/ลบข้อมูล
- เก็บ IP address และ user ที่ทำการเปลี่ยนแปลง
- ดูประวัติการเปลี่ยนแปลง

### 3. ความปลอดภัย

#### Data Protection
- ข้อมูลส่วนบุคคลแสดงเฉพาะ admin
- Public dashboard แสดงเฉพาะสถิติรวม
- Audit trail สำหรับการติดตาม

#### Access Control
- Admin functions ต้อง login
- Public dashboard เข้าถึงได้โดยไม่ต้อง login

## 🔧 การตั้งค่า

### Environment Variables

```bash
# Database
DATABASE_URL=sqlite:///hospital.db

# Admin credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_EMAIL=admin@hospital.local

# Security
SECRET_KEY=your-secret-key-here
```

### Database Schema

ระบบจะสร้างตารางใหม่โดยอัตโนมัติเมื่อรัน `migrate_cases.py`:

```sql
-- ตารางหลักสำหรับข้อมูลผู้ป่วย
CREATE TABLE patient_case (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    hn VARCHAR(20) NOT NULL,
    first_name VARCHAR(100) NOT NULL,
    last_name VARCHAR(100) NOT NULL,
    department_id INTEGER NOT NULL,
    case_date DATE NOT NULL,
    notes TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    created_by INTEGER,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    is_deleted BOOLEAN DEFAULT FALSE,
    FOREIGN KEY (department_id) REFERENCES department(id),
    FOREIGN KEY (created_by) REFERENCES admin_user(id)
);

-- ตาราง audit log
CREATE TABLE case_audit (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    case_id INTEGER NOT NULL,
    action VARCHAR(20) NOT NULL,
    user_id INTEGER,
    ip_address VARCHAR(45),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (case_id) REFERENCES patient_case(id),
    FOREIGN KEY (user_id) REFERENCES admin_user(id)
);
```

## 📱 การใช้งาน

### สำหรับผู้ใช้ทั่วไป

1. เข้าไปที่หน้าแรก `/`
2. คลิกปุ่ม "ดูสถิติ" เพื่อเข้าสู่ dashboard
3. เลือกช่วงเวลาและหน่วยงานที่ต้องการ
4. ดูสถิติและกราฟต่างๆ

### สำหรับ Admin

1. Login ที่ `/admin/login`
2. เข้าไปที่ "จัดการข้อมูลผู้ป่วย" ใน dashboard
3. เพิ่ม/แก้ไข/ลบข้อมูลผู้ป่วย
4. Export ข้อมูลเป็น CSV

## 🚨 ข้อควรระวัง

### การตรวจสอบข้อมูล
- HN จะถูกตรวจสอบว่าซ้ำในวันเดียวกันหรือไม่
- ข้อมูลที่ลบจะถูก soft delete (ไม่ลบออกจากฐานข้อมูลจริง)

### การ Backup
- ควร backup ฐานข้อมูลเป็นประจำ
- ข้อมูล audit trail ควรเก็บไว้อย่างน้อย 3 ปี

## 🔮 การพัฒนาต่อ

### Phase 1 (เสร็จแล้ว)
- ✅ ระบบบันทึกข้อมูลพื้นฐาน
- ✅ Public dashboard
- ✅ Admin management
- ✅ Export ข้อมูล

### Phase 2 (อนาคต)
- 📅 การเชื่อมต่อกับระบบอื่นๆ
- 📅 API สำหรับ external integration
- 📅 Advanced analytics และ ML
- 📅 Mobile app

### Phase 3 (อนาคต)
- 📅 Multi-hospital support
- 📅 Real-time notifications
- 📅 Advanced reporting
- 📅 Integration with HIS

## 📞 การสนับสนุน

หากมีปัญหาหรือต้องการความช่วยเหลือ:

1. ตรวจสอบ log files
2. ตรวจสอบ database schema
3. ตรวจสอบ environment variables
4. ติดต่อทีมพัฒนา

## 📄 License

ระบบนี้พัฒนาขึ้นสำหรับแผนกอายุรกรรม โรงพยาบาลสงฆ์

---

**เวอร์ชัน**: 1.0  
**อัปเดตล่าสุด**: {{ current_date }}  
**ผู้พัฒนา**: ทีมพัฒนาแผนกอายุรกรรม
