# คู่มือการใช้งานระบบสำรองข้อมูลอัตโนมัติ

## 📋 ภาพรวมระบบ

ระบบสำรองข้อมูลอัตโนมัติถูกออกแบบมาเพื่อช่วยป้องกันการสูญหายของข้อมูลสำคัญในระบบจัดการข้อมูลผู้ป่วยแผนกอายุรกรรม โดยระบบจะทำการสำรองข้อมูลทั้งฐานข้อมูล SQLite และไฟล์ที่ผู้ใช้อัปโหลดไว้ในระบบโดยอัตโนมัติ

## 🔄 ฟีเจอร์หลัก

1. **การสำรองข้อมูลอัตโนมัติ**
   - ระบบจะทำการสำรองข้อมูลตามระยะเวลาที่กำหนด (ค่าเริ่มต้น: ทุก 24 ชั่วโมง)
   - สำรองทั้งฐานข้อมูล SQLite และไฟล์ที่อัปโหลด

2. **การสำรองข้อมูลด้วยตนเอง**
   - ผู้ดูแลระบบสามารถสั่งให้ระบบทำการสำรองข้อมูลได้ทันที
   - เลือกได้ว่าจะสำรองเฉพาะฐานข้อมูลหรือไฟล์ที่อัปโหลด หรือทั้งสองอย่าง

3. **การจัดการไฟล์สำรองข้อมูล**
   - ดาวน์โหลดไฟล์สำรองข้อมูล
   - กู้คืนข้อมูลจากไฟล์สำรอง
   - ลบไฟล์สำรองข้อมูลที่ไม่ต้องการ

4. **การทำความสะอาดอัตโนมัติ**
   - ระบบจะลบไฟล์สำรองข้อมูลที่เก่าเกินกว่าระยะเวลาที่กำหนด (ค่าเริ่มต้น: 30 วัน)

## 🛠️ การตั้งค่าระบบ

### การเข้าถึงหน้าจัดการการสำรองข้อมูล

1. เข้าสู่ระบบด้วยบัญชีผู้ดูแลระบบ
2. คลิกที่ไอคอนเฟือง (จัดการระบบ) ที่มุมขวาบนของหน้าจอ
3. เลือก "จัดการการสำรองข้อมูล" จากเมนูดรอปดาวน์

### การตั้งค่าการสำรองข้อมูลอัตโนมัติ

1. ในส่วน "การสำรองข้อมูลอัตโนมัติ" คุณสามารถกำหนด:
   - **ระยะเวลาในการสำรองข้อมูล (ชั่วโมง)**: ความถี่ในการสำรองข้อมูลอัตโนมัติ (1-168 ชั่วโมง)
   - **เก็บข้อมูลสำรองไว้ (วัน)**: ระยะเวลาในการเก็บไฟล์สำรองข้อมูลก่อนที่ระบบจะลบทิ้งอัตโนมัติ (1-365 วัน)
2. คลิกปุ่ม "บันทึกการตั้งค่า" เพื่อบันทึกการเปลี่ยนแปลง

## 📋 การใช้งานระบบ

### การสำรองข้อมูลด้วยตนเอง

1. ในส่วน "การสำรองข้อมูลด้วยตนเอง" เลือกประเภทข้อมูลที่ต้องการสำรอง:
   - สำรองฐานข้อมูล
   - สำรองไฟล์ที่อัปโหลด
2. คลิกปุ่ม "สำรองข้อมูลทันที"

### การจัดการไฟล์สำรองข้อมูล

1. ในส่วน "ประวัติการสำรองข้อมูล" คุณสามารถเลือกดูข้อมูลได้ 2 แท็บ:
   - **ฐานข้อมูล**: แสดงรายการไฟล์สำรองฐานข้อมูล
   - **ไฟล์อัปโหลด**: แสดงรายการโฟลเดอร์สำรองไฟล์ที่อัปโหลด

2. สำหรับแต่ละรายการ คุณสามารถ:
   - **ดาวน์โหลด**: ดาวน์โหลดไฟล์สำรองข้อมูล
   - **กู้คืน**: นำข้อมูลจากไฟล์สำรองมาใช้แทนข้อมูลปัจจุบัน
   - **ลบ**: ลบไฟล์สำรองข้อมูลที่ไม่ต้องการ

## ⚠️ ข้อควรระวัง

1. **การกู้คืนข้อมูล**: การกู้คืนข้อมูลจะแทนที่ข้อมูลปัจจุบันทั้งหมด ควรใช้ความระมัดระวังและตรวจสอบให้แน่ใจว่าคุณเลือกไฟล์สำรองข้อมูลที่ถูกต้อง

2. **พื้นที่จัดเก็บ**: ไฟล์สำรองข้อมูลอาจใช้พื้นที่จัดเก็บมาก โดยเฉพาะถ้ามีไฟล์ที่อัปโหลดจำนวนมาก ควรตรวจสอบพื้นที่จัดเก็บเป็นประจำ

3. **การตั้งค่าความถี่**: การตั้งค่าความถี่ในการสำรองข้อมูลที่สูงเกินไป (เช่น ทุก 1 ชั่วโมง) อาจส่งผลต่อประสิทธิภาพของระบบ ควรตั้งค่าให้เหมาะสมกับความต้องการ

## 🔍 การแก้ไขปัญหาเบื้องต้น

1. **ไม่สามารถสำรองข้อมูลได้**
   - ตรวจสอบสิทธิ์การเข้าถึงโฟลเดอร์สำรองข้อมูล
   - ตรวจสอบพื้นที่จัดเก็บว่าเพียงพอหรือไม่

2. **ไม่สามารถกู้คืนข้อมูลได้**
   - ตรวจสอบว่าไฟล์สำรองข้อมูลไม่เสียหาย
   - ตรวจสอบสิทธิ์การเข้าถึงฐานข้อมูลและโฟลเดอร์อัปโหลด

3. **ระบบสำรองข้อมูลอัตโนมัติไม่ทำงาน**
   - รีสตาร์ทแอปพลิเคชัน
   - ตรวจสอบการตั้งค่าในไฟล์ .env

## 📞 การติดต่อสนับสนุน

หากคุณพบปัญหาในการใช้งานระบบสำรองข้อมูล กรุณาติดต่อผู้ดูแลระบบหรือทีมพัฒนา