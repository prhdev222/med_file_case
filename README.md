# 🏥 ระบบจัดการข้อมูลผู้ป่วย - แผนกอายุรกรรม

ระบบจัดการข้อมูลผู้ป่วยสำหรับแผนกอายุรกรรม โรงพยาบาล ระบบช่วยในการบันทึก จัดการ และติดตามข้อมูลผู้ป่วยในแต่ละหน่วยงาน

## ✨ ฟีเจอร์หลัก

### 🔐 ระบบจัดการผู้ใช้
- ระบบล็อกอิน/ล็อกเอาต์
- จัดการสิทธิ์ผู้ใช้ (Admin, Nurse, Doctor, Staff)
- เพิ่ม/ลบ/แก้ไขผู้ใช้

### 👥 ระบบจัดการผู้ป่วย
- บันทึกข้อมูลผู้ป่วย (HN, ชื่อ, นามสกุล, หน่วยงาน)
- อัปโหลดเอกสารแนบ (รูปภาพ, PDF)
- ลิงก์เอกสารภายนอก
- ระบบ Audit Trail

### 🏢 ระบบจัดการหน่วยงาน
- จัดการ 12 หน่วยงานหลัก:
  - **DM** - หน่วยเบาหวาน
  - **COPD** - หน่วยโรคปอดอุดกั้นเรื้อรัง
  - **UGIB** - หน่วยเลือดออกในทางเดินอาหารส่วนบน
  - **CKD** - หน่วยโรคไตเรื้อรัง
  - **STEMI/NSTEMI** - หน่วยกล้ามเนื้อหัวใจขาดเลือด
  - **STROKE** - หน่วยโรคหลอดเลือดสมอง
  - **TB** - หน่วยวัณโรค
  - **CHEMO** - หน่วยเคมีบำบัด
  - **HTN** - หน่วยความดันโลหิตสูง
  - **SEPSIS** - หน่วยภาวะติดเชื้อในกระแสเลือด
  - **RHEUMATO** - หน่วยโรคข้อและรูมาติสซั่ม
  - **OBESITY** - หน่วยโรคอ้วน

### 📊 ระบบรายงานและสถิติ
- แสดงสถิติผู้ป่วยตามหน่วยงาน
- กราฟวงกลมแสดงสัดส่วน
- กราฟเส้นแสดงแนวโน้ม 6 เดือน
- ส่งออกข้อมูลเป็น CSV

### 📁 ระบบจัดการเอกสาร
- อัปโหลดไฟล์รูปภาพและ PDF
- จัดการลิงก์เอกสารภายนอก
- ดาวน์โหลดเอกสาร

## 🛠️ เทคโนโลยีที่ใช้

- **Backend:** Python Flask
- **Database:** SQLite
- **Frontend:** HTML, CSS, JavaScript, Bootstrap
- **Charts:** Chart.js
- **Authentication:** Flask-Login

## 🚀 การติดตั้ง

### ข้อกำหนดระบบ
- Python 3.8+
- pip

### ขั้นตอนการติดตั้ง

1. **Clone repository**
```bash
git clone https://github.com/prhdev222/med_file_case.git
cd med_file_case
```

2. **สร้าง Virtual Environment**
```bash
python -m venv .venv
# Windows
.venv\Scripts\activate
# macOS/Linux
source .venv/bin/activate
```

3. **ติดตั้ง Dependencies**
```bash
pip install -r requirements.txt
```

4. **รันแอปพลิเคชัน**
```bash
python app.py
```

5. **เปิดเว็บเบราว์เซอร์**
```
http://localhost:5001
```

## 🔑 ข้อมูลเข้าสู่ระบบเริ่มต้น

- **Username:** admin
- **Password:** admin123

## 📁 โครงสร้างโปรเจค

```
med_file_case/
├── app.py                 # แอปพลิเคชันหลัก
├── templates/            # HTML templates
│   ├── admin/           # หน้าจัดการ Admin
│   ├── base.html        # Template หลัก
│   └── stats.html       # หน้าสถิติ
├── static/              # ไฟล์ Static (CSS, JS, Images)
├── instance/            # ฐานข้อมูล SQLite
├── storage/             # ไฟล์ที่อัปโหลด
├── tests/               # ไฟล์ทดสอบ
├── scripts/             # Scripts ต่างๆ
└── README.md            # เอกสารนี้
```

## 🧪 การทดสอบ

### รันการทดสอบทั้งหมด
```bash
python -m pytest tests/
```

### ทดสอบเฉพาะส่วน
```bash
python tests/test_cases.py
```

### สร้างข้อมูลตัวอย่าง
```bash
python restore_departments.py
```

## 📝 การใช้งาน

### สำหรับ Admin
1. เข้าสู่ระบบด้วย admin account
2. จัดการผู้ใช้ในระบบ
3. จัดการหน่วยงาน
4. ดูรายงานและสถิติ

### สำหรับเจ้าหน้าที่
1. เข้าสู่ระบบด้วย account ที่ได้รับ
2. เพิ่ม/แก้ไขข้อมูลผู้ป่วย
3. อัปโหลดเอกสารแนบ
4. ดูข้อมูลผู้ป่วยในหน่วยงาน

## 🔧 การบำรุงรักษา

### ตรวจสอบฐานข้อมูล
```bash
python check_db_structure.py
```

### เพิ่มฟิลด์ที่ขาดหายไป
```bash
python add_missing_fields.py
```

### จัดการ Users
```bash
python add_user.py
python delete_user.py
```

### จัดการหน่วยงาน
```bash
python manage_departments.py
```

## 📞 การติดต่อ

- **Developer:** prhdev222
- **Email:** uradev222@gmail.com
- **Repository:** https://github.com/prhdev222/med_file_case

## 📄 License

โปรเจคนี้พัฒนาเพื่อใช้ในระบบโรงพยาบาล

---

**พัฒนาโดย:** prhdev222  
**วันที่:** 2025  
**เวอร์ชัน:** 1.0.0
